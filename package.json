{
  "name": "iis-scheduler",
  "version": "0.0.0",
  "description": "iis-scheduler",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "IIS",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
